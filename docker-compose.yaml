version: '3.9'

services:
  postgres:
    image: postgres:17
    container_name: galleria_bank_db
    restart: unless-stopped
    environment:
      POSTGRES_DB: galleria_bank
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"